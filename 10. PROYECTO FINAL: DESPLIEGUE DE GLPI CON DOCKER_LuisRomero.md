# TEMA 10 – PROYECTO FINAL: DESPLIEGUE DE GLPI CON DOCKER - Luis Romero Luque (2ºASIR)

Guia del desarrollador para desplegar GLPI – Repositorio oficial: https://github.com/glpi-project/glpi

## 1. Introducción

El proyecto final del curso consiste en el despliegue completo de una aplicación real de uso profesional utilizando contenedores Docker.

La aplicación seleccionada es GLPI, una herramienta utilizada en entornos empresariales para la gestión de servicios TI (ITSM), inventario y soporte técnico.

Este proyecto no utiliza Kubernetes.

## 2. Explicación de la arquitectura

La arquitectura del proyecto está compuesta por dos contenedores principales conectados mediante una red privada Docker. El contenedor 'glpi_app' ejecuta el servidor web Apache con PHP y la aplicación GLPI, actuando como frontend accesible desde el navegador. 
El contenedor 'glpi_db' ejecuta MariaDB, funcionando como backend para almacenar toda la información de usuarios, incidencias, inventario y configuración. 
Ambos contenedores se comunican mediante la red interna 'glpi_net'. 
Además, se utilizan volúmenes persistentes para garantizar que los datos no se pierdan tras reiniciar o eliminar los contenedores.

## 3. Archivo docker-compose.yml

    services:
      db:
        image: mariadb:10.6
        container_name: glpi_db
        restart: always
        environment:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: glpi
          MYSQL_USER: glpi
          MYSQL_PASSWORD: glpi
        volumes:
          - db_data:/var/lib/mysql
        networks:
          - glpi_net
    
      glpi:
        image: luiromluq/glpi:latest
        container_name: glpi_app
        restart: always
        ports:
          - "8080:80"
        environment:
          TIMEZONE: Europe/Madrid
        volumes:
          - glpi_data:/var/www/html/glpi
        depends_on:
          - db
        networks:
          - glpi_net
    
    volumes:
      db_data:
      glpi_data:
    
    networks:
      glpi_net:
        driver: bridge


## 4. Capturas:
### 4.1. Contenedores en ejecución

<img width="1210" height="157" alt="image" src="https://github.com/user-attachments/assets/701bcc46-b184-456e-93b6-6c01e5f87a60" />

Los contenedores GLPI y MariaDB están en estado "running", lo que nos indica que están correctamente desplegados.

### 4.2. Acceso a GLPI

<img width="1214" height="724" alt="image" src="https://github.com/user-attachments/assets/28224267-8e0b-452d-be61-130859e9d8b8" />

Vemos que se ha podido acceder a GLPI desde el navegador con la dirección http://localhost:8080

## 5. Explicación del uso de volúmenes


## 6. Conclusiones personales
